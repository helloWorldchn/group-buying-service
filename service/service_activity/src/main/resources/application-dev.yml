server:
  port: 8204

mybatis-plus:
  configuration:
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl
  type-enums-package: com.example.groupbuying.enums
  mapperLocations: classpath:com/example/groupbuying/activity/mapper/xml/*.xml

spring:
  datasource:
    type: com.zaxxer.hikari.HikariDataSource
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/groupbuying-activity?characterEncoding=utf-8&useSSL=false
    username: root
    password: 121314

  jackson:
    date-format: yyyy-MM-dd HH:mm:ss
    time-zone: GMT+8